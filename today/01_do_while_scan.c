#include <stdio.h>
#include <math.h>
int main()
{
    int num;
    do
    {
        printf("enter number : ");
        scanf("%d", &num,&);
        printf("printf number : %d\n", num);
    } while (num != 0);
    return 0;
}